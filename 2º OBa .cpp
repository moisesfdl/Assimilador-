//'©'moisesfdl_pbt@hotmail.com.br
#include <activation.h>
#include <>
#include "OBa"

#define OBa;
OBa{ CPU == OBa[i5] '+=1' };


long main()
{
    &Ref moisesfdl;
    *(*CPU)ptr;
    Vetor[i5]{ moisesfdl };
    Vetor{ i5 };
    OBa{ auto("audio" + ouvir)auto("ouvir" + dialeto)auto("dialeto" + log),sizeof("log" + dialeto),sizeof("log" + analise),sizeof("log" + responder)};
    std::cout << "CPU [" << CPU('OBa') << "] : " << std::endl;
    if (void)
    {
        *(*OBa)ptr;
        auto{OBa("audio" + log())};
        OBa{log(audio)OBa{analise{"log()"+responde}}};
        auto{OBa + "responder" + moisesfdl};
        ptr{auto(void())};
    };  
    for (OB i = 0; i > 0; i++)
    {
    OBa{signbit(LONG64)?sizeof(void):OBaJ_ENHMETAFILE(LOG)};
    OBa '+=1';
    };
        std::cout << "CPU " << auto{void()} << " :" << std::endl;
    OBa{ auto("audio" + ouvir)auto("ouvir" + dialeto)auto("dialeto" + log),sizeof("log" + dialeto),sizeof("log" + analise),sizeof("log" + responder)};
    system("CPU");
    return OBa;
};
